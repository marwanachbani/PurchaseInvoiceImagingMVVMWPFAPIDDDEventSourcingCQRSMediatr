<UserControl x:Class="WpfUI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:WpfUI.ViewModels" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFEDF9FB" >
        <TextBox Text="{Binding UserName , UpdateSourceTrigger=PropertyChanged}" Margin="250,170,250,250" Width="300" Height="30"/>
        <TextBox Text="{Binding Password , UpdateSourceTrigger=PropertyChanged}" Width="300" Height="30" Margin="250,225,250,195"/>
        <Button Command="{Binding LoginCommand}" Content="sign in" Width="100" Height="30" Background="#FF0C2C98" Margin="450,337,250,83" Foreground="#FFF1E8E8">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</UserControl>
